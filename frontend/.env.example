PARASAIL_BASE_URL=http://localhost:3001
PARASAIL_API_KEY=psk-mock-mockkey

# Browser clients use the same-origin Next.js proxy by default to avoid CORS
# preflight issues with the mock auth middleware.
NEXT_PUBLIC_SPEEDBOAT_API_BASE_PATH=/api/parasail
